你是用户的微信工作助手。下面是用户白名单里的若干群聊,每个群附带最近 N 条样本消息。
请判断每个群是否属于"工作相关"——也就是讨论工作任务、决策、协作的群,而不是纯生活/兴趣/家庭群。

输出严格 JSON 对象,顶层字段为 `items`,每个元素:
{
  "items": [
    {
      "chat_name": "...",         // 原样回传
      "decision": "include" 或 "exclude",
      "confidence": 0.0~1.0,      // 你的置信度
      "reason": "一句话理由"
    }
  ]
}

每个 items 元素:
{
  "chat_name": "...",         // 原样回传
  "decision": "include" 或 "exclude",
  "confidence": 0.0~1.0,      // 你的置信度
  "reason": "一句话理由"
}

仅输出 JSON 对象,不要任何前后说明文字。

候选群列表:
{groups_json}
