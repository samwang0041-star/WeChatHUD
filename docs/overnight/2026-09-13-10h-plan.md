# WeChatHUD 10h 长期提升方案（2026-09-13 14:00 → 00:00）

起点：`48d6b3d`（PR #5 / `overnight-pr5` → `cursor/overnight-evolution-266d`）  
模式：本机 Mac 编辑 + `swift test` + `swift build -c release` + push。不用 Codex / Cursor Cloud。  
节奏：每约 20 分钟一刀；到点（Asia/Shanghai **2026-09-14 00:00**）收工并删例行。

## 硬约束
- Autopilot 护栏不放宽：默认关、confidence 0.8、敏感词、财务待审、群聊人工确认、session cap 50。
- 可选 AI 趋势文案仍默认关；单日 insight 不注入 `attitudes` / `tone_changes` / `mood_shift`（无产品 opt-in）。
- UI 文案 zh-Hans；不提交 `docs/overnight/*codex*` 噪音稿。

## 优先队列（按刀推进）

### A. WeChatReaderActor 扩面（P1 续）
1. Insight 读路径：`InsightDataLoader` / `InsightStore` / `ChatInsightService` 热路径改走 actor 或 batch。
2. ChatMonitor 仍直接持 reader 的 off-main 辅助读，能迁则迁；迁不动就记债。
3. Autopilot 只读消息路径评估是否可 `messagesBatch`（发送/剪贴板已 MainActor，勿回退）。

### B. HUDStore 收尾
1. 迁移列探测（migration col-checks）是否还能收口。
2. 剩余 cache internals 文档化；不半吊子大改 schema。
3. 有把握的小写路径继续 `withCachedStatement`（已完成 hot autopilot writes）。

### C. 质量与产品边角（穿插）
1. 清掉可复现的小崩/测试债（优先 Range/并发相关回归）。
2. 文档：`docs/overnight/2026-09-13-progress.md` + `evolution-log` 每刀追加。
3. 每刀：全量 `swift test`（目标 ≥1522+70）+ release；失败不推。

## 明确不做（本 10h）
- 不重写整个 WeChatReader 为纯 actor（facade 渐进即可）。
- 不接新云端模型、不放宽自动驾驶。
- 不在 Linux/cloud 上编 Swift。

## 验收
- 分支持续领先并推到 `origin/cursor/overnight-evolution-266d`。
- 到点 zh-Hans 简报：合入 commit 列表、测数、未完成债。
