# Overnight progress — 2026-09-13 (local Mac)

Branch `overnight-pr5` → `cursor/overnight-evolution-266d` (PR #5).

## Slice 1 — macOS test compile + guardrail honesty

`swift build -c release` was already green at `c51c2aa`. `swift test` did not compile on Mac.

Fixed:

- `AutopilotGuardrailPipelineTests`: hoist actor-isolated `sessionSent` out of `XCTAssert*` autoclosures.
- `SecurityRegressionTests`: optional `AIError` pattern match (`error as? AIError` is `AIError?`).
- Group `@` with `handleGroupAt=true` still queues `manualOnlyReason = 群聊消息，请人工确认后发送`, and the log action is now `.pending` instead of claiming `.sent`.
- AI audit assertions use `AIAuditPrivacy.persistableText` (`sha256:` + snippet).

Verification on this Mac:

| Command | Result |
|---|---|
| `swift build -c release` | pass (existing `InsightDataLoader` Sendable warning only) |
| `swift test` | 1512 XCTest + 70 swift-testing, 15 skipped, 0 failures |

Autopilot defaults unchanged. Group / finance / keyword / session-cap holds were not weakened.

## Slice 2 — Wave C: dedicated 关系雷达 pane + scan-tick refresh

Shipped:

- Workspace tab `SettingsView.Tab.relationshipRadar`（回顾 → 关系雷达）. Full pane lists ranked snapshots (转淡 / 沉默 first), filters, dark signals, tone trail. Compact `RelationshipRadarCard` stays on the insight overview and chat-detail overview.
- `RelationshipRadarService.refreshAll` rebuilds snapshots from stored daily facts. 15-minute throttle on the scan hop; the pane refresh button forces. Single-chat analyze writes a fact then refreshes that chat immediately.
- ChatMonitor successful scan awaits `refreshRelationshipRadarAfterScan()` off-main **before** publishing `lastSyncAt`, so an open pane reloads already-new silence days.
- No AI trend language, no outbound send, no Autopilot change. Single-day insight still has no `attitudes` / `tone_changes` / `mood_shift`.

Also fixed a latent crash: `buildSnapshot` used `1..<moods.count` when every day's `overallMood` was empty (`1..<0`). Scan-tick/analyze refresh now hits that path, so the range is gated on `moods.count >= 2`.

Tests: `RelationshipRadarTests` (throttle, silence-from-clock, rank order, empty mood, ChatMonitor off-main hop), `HistoricalInsightReliabilityTests`, `ProductAccessibilityTests` / `CompanionProductCopyTests` tab copy.

## Slice 3 — Wave B leftover: more HUDStore paths on the serial queue

Whitelist / `sync_state` / contacts / `chat_actions` reads now go through `queryOne`/`queryAll` → `withDatabaseMutex` → `com.wechathud.hudstore`. Nested `perform` stays re-entrant.

Still direct `sqlite3_prepare_v2` (FULLMUTEX only): drafts, autopilot log/queue, classification queue, ignored senders, VIP traces, conversation memory, analysis cache, AI audit.

Tests: `HUDStoreTests` (67), `HUDStoreConcurrencyAndMigrationTests` mixed whitelist/sync/contacts/radar stress.

Verification after this slice:

| Command | Result |
|---|---|
| `swift test` | 1518 XCTest + 70 swift-testing, 15 skipped, 0 failures |
| `swift build -c release` | pass |

## Slice 4 — drafts / autopilot / classification readers + cache reset fix

Shipped:

- `getSetting`, `latestDiscussionSourceTimestamp`, `draftCount` / `loadComposerDrafts` / `loadDrafts`, `pendingClassificationMessages` / `classificationQueueCount`, `currentAutopilotSession` / `loadAutopilotSessions`, `loadAutopilotLog` / `loadPendingAutopilotItems`, `loadPendingAutopilotInbound` now go through `queryOne`/`queryAll`/`scalarCount` (serial queue + statement cache).
- Shared decoders: `decodeAutopilotSession`, `decodeAutopilotLogEntry`.
- **Fix:** `withCachedStatement` now `sqlite3_reset`s after every successful use. Leaving a cached `queryOne` on `SQLITE_ROW` pinned a read transaction, so later `normalizeDiscussionDueDates` could not see TEXT `due_at` writes from another connection (`DiscussionDueAtStorageTests`).

Still direct prepare (next slices): open autopilot pending, pending sends, ignored senders, pending asks, AI audit, analysis cache, VIP traces, recalled messages, conversation memory, reply timing, relationship profiles, `HUDStore+DiscussionQueue`, WeChatReader.

Verification:

| Command | Result |
|---|---|
| `swift test` | 1518 XCTest + 70 swift-testing, 15 skipped, 0 failures |
| `swift build -c release` | pass |

## Slice 5 — ignored/dismissed/asks/cache/memory/pending sends/profiles

Shipped more readers onto `queryOne`/`queryAll`:

- ignored senders + `isSenderIgnored`, group member rules
- scan dismissed list / username set, `hasPendingAsk`
- analysis cache, conversation memory, reply timing profile
- `loadPendingSends`, relationship profile get/all
- `loadOpenAutopilotPendingItems` as four fixed SQL variants (keeps statement cache hot)
- dropped duplicate `autopilotLogEntry(from:)` in favor of `decodeAutopilotLogEntry`

Still direct prepare: pending asks list (dynamic filters), AI audit/feedback (dynamic), VIP traces, recalled messages, write prepares (`insertAutopilotLog` / upsert pending send / enqueue inbound), migration col-checks, `HUDStore+DiscussionQueue`, WeChatReader.

Verification:

| Command | Result |
|---|---|
| `swift test` | 1518 XCTest + 70 swift-testing, 15 skipped, 0 failures |
| `swift build -c release` | pass |

## Next

- VIP traces / recalled / AI audit-feedback / pending asks list
- Optional live AI trend language behind an explicit opt-in (not default)
- WeChatReader actor boundary
