# Overnight progress — 2026-09-13 (local Mac)

Branch `overnight-pr5` → `cursor/overnight-evolution-266d` (PR #5).

## Slice 1 — macOS test compile + guardrail honesty

`swift build -c release` was already green at `c51c2aa`. `swift test` did not compile on Mac.

Fixed:

- `AutopilotGuardrailPipelineTests`: hoist actor-isolated `sessionSent` out of `XCTAssert*` autoclosures.
- `SecurityRegressionTests`: optional `AIError` pattern match (`error as? AIError` is `AIError?`).
- Group `@` with `handleGroupAt=true` still queues `manualOnlyReason = 群聊消息，请人工确认后发送`, and the log action is now `.pending` instead of claiming `.sent`.
- AI audit assertions use `AIAuditPrivacy.persistableText` (`sha256:` + snippet).

Verification on this Mac:

| Command | Result |
|---|---|
| `swift build -c release` | pass (existing `InsightDataLoader` Sendable warning only) |
| `swift test` | 1512 XCTest + 70 swift-testing, 15 skipped, 0 failures |

Autopilot defaults unchanged. Group / finance / keyword / session-cap holds were not weakened.

## Next

- Wave C leftover: dedicated relationship-radar pane + scan-tick refresh
- Wave B leftover: more HUDStore paths on the Swift serial queue
