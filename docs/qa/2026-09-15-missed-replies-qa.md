# 没回的 / 视觉精修质检（2026-09-15）

对象：当前工作树（未发布）。预览进程 `WeChatHUD Preview`，`--preview-tab=today`，界面自述「全部为虚构数据」。
方法：源码对抗 + `swift test` + 真实点击 AX（需要回复 → 没回的 → 今天 / 近 3 天 / 近 7 天 → 理解上下文与回复）。

## 结论

功能在预览里能点通，时间范围会改列表。质检中修了两条会让列表身份崩溃的插值错误。
**不能**把这次预览当成真实微信库验收。本机 `/Applications/WeChatHUD.app` 仍是 1.5.4，不含此功能。

测试：1641 XCTest + 70 Swift Testing，0 失败，11 个显式跳过。

## 实测

| 步骤 | 结果 |
|---|---|
| 今天首屏 | 侧栏是符号 + 玉色选中，不是彩虹方块。`没回的，2 项` 可见。 |
| 点没回的 | 标题「这段时间有人找你」；近 7 天默认选中；许宁（2 条）+ 项目协作群（@ 我）。 |
| 点今天 | 空态「没有没回的消息」；胶囊变成 `没回的，0 项`。演示数据都在 2–3 天前，符合过滤。 |
| 点近 3 天 | 只剩项目协作群 @。许宁（3 天前）被拿掉。 |
| 理解上下文与回复 | 打开浮岛对话 `preview-project · 群聊`。正文是预览会话的近讯，**不是**那条没回的 @。 |

## 质检中已修

- 所有漏回条目曾共用字面量 id `(seed.session.username)|(first.message.id)`，ForEach 会串台。现为 `chat|message`。
- 「N 条」徽章曾显示字面量 `(item.unrepliedCount) 条`。
- 预览里切换时间原先短路、列表不变；现按时间过滤演示数据。
- 扫描改为逐会话读取，一条 SQL 失败不会整页空白。

## 未闭环

1. **真实微信库未扫。** 预览不读微信。本机 `/Applications/WeChatHUD.app` 仍是 1.5.4。
2. 未关注、又不在微信最近会话里的群，不在扫描范围。
3. 每个对话该时间窗最多看最近 400 条。

## 质检后已处理（同日）

- 页头切到没回的时改为「指定时间里还没回的私聊和群 @」。预览 AX：`今天 … 指定时间里还没回的私聊和群 @`。
- 「理解上下文与回复」带上原话焦点。预览打开 `preview-xu · 私聊` 时正文是「上周说的报价，你看了没？我这边要交差。」，不再是会话近讯。
