# WeChatHUD 产品化验收 — 2026-09-08

本报告区分源码验证、原生演示操作、真实数据读取、实时 AI 服务语义验收和公开分发。演示成功不能证明真实微信发送；实时服务返回成功也不能单独证明真实聊天内容已被正确理解。

## 已取得的证据

- 真实只读诊断：当前账号目录 1 个，联系人 12,313 个，会话 2,721 个；优先探测关注会话后读取到 1 条实际消息，探测失败 0。输出不包含消息正文、姓名、路径或密钥。见 `live-source-check.json`。
- 真实 AI 连接测试：用户补充配置并指定 Flash 后，发行包使用 `deepseek-v4-flash` 发送合成测试文本，连接成功。此前鉴权失败已解除；未发送真实聊天内容。见 `live-ai-check.json`。
- DeepSeek Flash 语义验收：使用真实 `deepseek-v4-flash` 服务对虚构项目群输入完成 7 项服务级检查，6 项正向语义检查通过，另 1 项为反向/负向方向性检查并通过。覆盖群 @ 识别、群聊追赶、单聊洞察、回复建议、托管回复决策、我的承诺识别，以及“对方动作不能倒灌成我的承诺”。此前模型把“林晓收到后告诉我”反向写成“我告知林晓”；修复后的提示词保留执行人和动作方向，负向断言确认不再产生该倒置结果。结果与输入均为合成数据，不代表真实消息投递。详见 `synthetic-companion-ai.md`。
- 承诺处理修复：承诺执行人方向已固定；截止时间解析已按消息日期解析“今天/明天/后天”、中文时刻、绝对日期和日期末尾，无法确定或格式冲突时保持未解析，不静默改成下一天。该逻辑与实时语义验收一并由现有测试覆盖。
- 最终全量检查：XCTest 705 项（2 项按设计跳过）与 Swift Testing 69 项，共 774 项；772 项通过、2 项跳过、0 项失败。覆盖严格候选来源、缺失密钥/数据库、合法空联系人、取消与部分读取失败、AI 能力持久化、联系人/待办草稿、承诺完成与恢复、截止时间解析和实时语义验收。
- 真实加密数据库 fixture 经过 ScanEngine 验证分类、讨论和托管入队；注入 SQLite trigger 拒绝写入后游标不推进，重试成功后推进，重复扫描不重复入队。

## 原生窗口操作记录

以下操作在独立 `com.wechathud.product-preview` 中使用虚构资料完成，没有操作真实微信或申请系统权限。最终原生窗口在 820 点宽度下从上到下复验了导航、Today 和各设置入口；截图证据包括 `05AI`、`06contacts820`、`07insight820`、`08-today-compact.png` 和 `09-tasks-compact.png`。

- 统一工作台：Today、收件箱、联系人、洞察、承诺和设置使用统一的原生卡片与导航，820 点宽度下内容可滚动且不被底部操作遮挡；AI 区块可折叠，折叠状态和设置保存后重新进入仍保持。Today 统计标题在紧凑宽度下已修正换行，见 `08-today-compact.png`。
- 今天：显示原文入口、上下文/回复入口、关注范围、尚无成功同步；没有把演示数据当作同步成功。Discussion 筛选与记录 toggle 已拆成两行，来源元数据与动作也分行展示，紧凑窗口下仍可读，见 `09-tasks-compact.png`。
- 待办与草稿：待办点击完成后列表由 3 项变 2 项并显示“事项状态已保存”回执，撤销后恢复为 3 项；侧栏数量与列表一致。修改回复草稿后显示自动保存，离开页面再返回仍保留新文本。
- 承诺：820 点宽度下可见原话、背景、对象、期限、下一步。点击完成后进行中 1→0、已完成 0→1，已完成记录保留原话并出现在侧边栏/筛选计数；恢复后回到进行中，列表、侧边栏和计数一致。
- 联系人推荐扫描：加载密钥并刷新联系人后才进行排名；数据库读取失败与合法空候选分开反馈，任务在后台执行并支持取消。最终原生预览点击“开始扫描”时，缺少密钥显示“候选联系人读取失败。请检查微信连接和数据目录后重试。”，不再显示空候选。
- 联系人：修正类别标签被挤成竖排；编辑备注保存后列表与详情同时更新。VIP 同事角色原先不在选项中导致空白，现已解除角色与关注级别的不必要绑定；最终原生表单已显示“VIP / 同事”且备注持久保留。
- AI 与提醒：控制项具备可访问标签，单层滚动；群 @ 开关保存后显示即时生效回执。真实非 VIP 提醒入口由扫描 fixture 单独验证。
- 托管：浅色窗口文字可读，默认无人值守发送关闭；托管会话与发送策略分开说明，并明确灰名单已建档私聊也属于托管范围。
- 指南：可打开首次介绍；点击连接设置后介绍弹窗关闭，正确进入连接页，无遮挡。
- 连接：显示当前/下次启动目录、生效时机、密钥状态、成功同步时间；演示模式禁止登录项和系统权限变更。最终重建预览已验证缺目录时显示“未找到微信数据目录 / 需处理 / 选择数据目录”；修改隔离演示目录后立即显示待重启、说明本次运行仍使用旧目录，主按钮禁用并标为“重启后生效”。演示路径已恢复 auto，真实用户配置未改。截图：`design/10-connection-compact.png`、`design/11-connection-restart.png`。
- 洞察：0 消息时显示数据不足说明，刷新/复制禁用，隐藏“没有异常”和无依据的满分指标。
- 日报：有来源时规则结果标为“规则整理”，不冒充 AI；最终原生草稿已显示“本地记录”而非编造完成工作；已重新导出到桌面并验证文本。空来源逻辑另有定向测试。

## 发行包验证

- 最终包内 `bundle-check` 实际执行通过，动态库和内置提示词均可用；该命令不访问账号、设置、网络或 GUI。
- zip 解压到独立临时目录后再次通过；移走该副本内的提示词 bundle，命令按预期返回退出码 1，证明没有借用开发目录资源。见 `portable-smoke.json`。
- App 与固定源码编译的 zstd 均为 arm64，最低系统声明为 macOS 14.0；动态依赖不包含 Homebrew 绝对路径。嵌套签名和 SHA-256 验证通过。
- 曾发现本地签名启用 hardened runtime 后被 dyld 拒绝，以及 Homebrew zstd 要求 macOS 26 的两个真实分发缺陷；均已修复并以包内实际运行复验。Developer ID 发行仍保留 hardened runtime。
- `live-source-check.json` 已由成功启动的发行包程序重新生成。

## 尚未通过的外部门槛

- DeepSeek Flash 的 7 项服务级语义验收已完成，但输入仍是合成项目群；真实聊天摘要、真实群 @ 上下文回答、真实承诺 AI 识别和真实发送回执仍需真实数据与明确测试收件人，不能由服务连接成功或合成语义通过替代。
- Developer ID 签名、Apple 公证和 Gatekeeper 已通过；全新 Mac 的安装与使用验收仍未完成。
- 已经用户明确授权向“测试号”发送一次指定测试消息，微信原生界面显示消息气泡且输入框清空；此项仅验证微信发送操作，WeChatHUD 自身发送入口与回执状态仍未完成实测。
- 旧版资料仍提示账号归属需确认；没有擅自将旧资料绑定到当前账号。

安装包已通过 Apple 的分发安全校验，可以交付安装测试；上述真实使用与跨设备验收仍需继续，不以公证代替功能验收。

## 最终交付复核（2026-09-08 12:59）

- 最终全量 debug/test/release/package 均通过；release 无编译警告。两个常规跳过用例分别为不可用的本地 AI 端点和显式 opt-in 的语义验收；后者已单独启用并通过七项真实服务检查。
- 包内只读源自检于 04:57:42Z 更新，AI 自检于 04:57:52Z 更新，证据分别见 `live-source-check.json` 与 `live-ai-check.json`。
- 模型列表搜索 flash 后只剩当前 Flash；Tab、Return 可确认并关闭列表，配置与验证状态保持一致。
- 演示来源讨论明确说明没有真实原文，可关闭返回，未制造原文。
- 最终桌面首页截图：`design/12-final-today.png`；窗口保持打开供查看。
- ZIP SHA-256：`bdc7da7a67cd83407e0036d5d035cc30d9b5da37bb8ec385c14d2c7cd481d1cb`。

## Developer ID 与公证完成（2026-09-08 13:27）

- 新 Developer ID Application 证书已在本机与私钥配对；签名使用 hardened runtime、Apple 时间戳和完整证书链。
- Apple 公证提交 `a2c5cae0-00f1-4e74-b55c-14e1ab6d4181` 状态为 **Accepted**。
- 公证票据已附加到 `.app`，重新生成 ZIP 后，在独立临时目录解压复验：严格签名、公证票据、Gatekeeper、包内资源检查全部退出码 0。
- Gatekeeper 返回 **accepted / source=Notarized Developer ID**。证据见 `public-distribution-check.json`。
- 当前 ZIP SHA-256：`bdc7da7a67cd83407e0036d5d035cc30d9b5da37bb8ec385c14d2c7cd481d1cb`。
- 早先“无有效身份”的检查仅覆盖默认钥匙串；旧钥匙串实际有发行身份，但私钥无法解锁。用户在 Apple 网站申请新证书后已完成正式签名，此前 rejected 状态已解除。

本机安装验收：已将公证版本放入 `/Applications/WeChatHUD.app`，严格签名、Gatekeeper、包内资源检查通过；安装后只读源诊断仍可读取微信数据库。证据 `installed-app-check.json`。这不替代全新 Mac 验收。

## 经授权的微信发送检查（2026-09-08）

- 用户指定联系人“测试号”，并明确批准发送一次「WeChatHUD 发送测试，请忽略。」。
- 用户打开会话后，通过微信原生辅助功能界面核对当前会话标题与输入内容，点击一次“发送”。
- 操作后同一会话出现对应消息气泡，输入框清空、“发送”按钮恢复禁用；未观察到失败提示，未重复发送。
- 此证据不证明对方已读，也不替代 WeChatHUD 产品自身的发送入口、账号保护和回执闭环验收。

## 正式安装版 GUI 真实同步（2026-09-08 13:38）

- 退出旧 distribution 构建进程后，独立运行 `/Applications/WeChatHUD.app`。首次 GUI 显示目录不可读，与此前终端自检结果不同，证实原生应用文件权限需要独立验收。
- 用户明确授权读取当前微信数据目录后，通过应用内 macOS 文件选择器选择当前 `db_storage`，重新检测后显示目录可读。
- 点击“立即同步”后，连接页显示“微信连接正常 / 已连接”，最近成功同步时间为 13:37:57。
- 退出并重新启动正式安装版，浮窗仍显示“微信连接正常”，工作台显示最近同步 13:38；本机重启应用后的访问保持有效。
- 没有绑定旧版资料，没有再次发送微信消息；真实 AI 内容质量与产品发送回执仍需继续验收。

## 安装后体验修正（源码，2026-09-08 13:41）

- 文件选择器确认目录后立即刷新连接诊断，避免授权已成功仍显示旧的目录不可读状态。
- 登录项 `.notFound` 不再直接解释为应用未安装；已从系统或用户 Applications 目录运行时，提示检查系统登录项。
- `swift build` 与 `SyncConnectionDiagnosisTests` 5 项通过，`git diff --check` 通过。
- 以上两项仍为源码修正，尚未替换当前已公证安装包；下次发行必须重新构建、签名和公证。

## 1.1.1 候选验收（2026-09-08 14:30）

- 连接设置与首次向导使用同一个原生连接组件：预定位授权窗口、授权后检查、应用连接后自动重新打开。主页面不再要求用户填写目录或 JSON；技术诊断移入折叠区域。
- 新增连接目标选择与重新打开保护；新实例等待原实例退出后才启动后台监控。
- 修复真实“测试号”洞察中总消息 0、我的消息 1、其他人 −1 的错误。总量与比例统一来自本地统计，不再使用 AI 话题计数。
- 单条可读消息被当成“暂无可读内容”的旧提示词已升级为 `chat_insight_v3`，同时更新缓存和审计版本。真实 DeepSeek Flash 单条测试回归与原有七项合成服务检查通过。
- 全量：719 项 XCTest（3 项默认跳过）+ 69 项 Swift Testing，共 788 项、0 失败；其中两项真实 AI 用例已另行显式启用并通过。Release 无警告。
- 候选公证 `be0d24bb-101c-40d9-a5ca-7a5570c13add` Accepted；票据、严格签名、Gatekeeper 和搬迁资源检查通过。证据 `distribution-1.1.1-check.json`。候选 SHA-256：`832315708e8f34f11bfe040d416f04f4dd13f7b073a2b4073e8dbfb3683773f6`。
- 此候选尚未替换正式安装版：复核发现进入洞察页会隐式触发全量关注会话 AI 分析，历史日期的详情统计也可能仍取当前窗口范围。两项须继续修复后再进行测试号正式版验收。
- 全新 Mac 无现成读取资料时，首次连接准备仍没有安全、自动化的完整实现。该限制尚未解决，不能宣称普通用户首次使用已验收通过。

## 洞察日期与引导回归（2026-09-08）

- 进入洞察页、“今日态势”导航仅加载本地统计；完整 AI 分析保留在明确的分析/刷新操作中。
- 单会话详情统计改为所选自然日的完整本地消息，包含日初、不包含次日零点；关注与其他会话使用相同读取逻辑。同步成功后刷新统计缓存。
- 全量简报同样使用完整当天统计，修复仅从最近 200 条计算的遗漏。AI 分析仍保留现有 500 条覆盖说明。
- 单聊请求按版本和日期隔离展示；旧请求不能覆盖新请求；全量分析不覆盖正在进行或已显示的历史日分析。
- 首次介绍与 AI 设置继续精简，连接状态区分保存与验证，自定义服务地址放入高级设置。原生预览检查了连接页和 AI 引导页，截图见 `design/16-onboarding-ai-final.png`。
- 独立回归新增加密 SQLite 日边界 fixture、AI 请求乱序完成测试。全量 792 项（XCTest 723、Swift Testing 69），3 项默认跳过，0 失败，日志 `/tmp/wechathud-historical-insight-full-tests.log`。随后全量合并增加进行中/历史日保护，需以最后的定向测试和发行校验补充。
- 首次连接能力复核：现有读取器依赖已有解密材料；文件授权不产生该材料。CLI 初始化涉及微信进程内存与改签名路径，未集成或执行。全新 Mac 一步连接仍未实现，普通用户首次连接验收仍不通过。

### 最终发行与正式版实测

- 最后合并保护后的 `HistoricalInsightReliabilityTests` 9 项通过；Release 65.92 秒完成、无警告。
- 最终 Apple 公证 `9a8f74a7-1b1c-4b6c-a9a4-86638102feef` Accepted。已附票据、重新压缩，并在独立临时目录解压验证严格签名、票据、Gatekeeper、资源自检，全部退出 0。
- 最终 1.1.1 ZIP SHA-256：`154af0f65017af44aec9529cd391a78d167099a3283fa6cd5b6b480bd3171788`。此项取代上文旧候选哈希。
- 已备份原 1.1.0 应用并安装至 `/Applications/WeChatHUD.app`。原生启动后最近同步显示 14:56，读取权限保持有效。
- 打开洞察总览时仍显示“开始分析”，没有隐式全量 AI 作业。仅点击测试号：当天消息总量 1、我的消息 1、其他人 0；AI 判定为发送测试，无需行动。
- 原生日期控件切到 9 月 7 日：统计为 0、0、0，今天的 AI 结果消失；返回 9 月 8 日后统计和测试分析恢复一致。截图 `design/18-installed-test-insight.png` 仅保留测试号筛选结果。
- 本轮未发送新的微信消息，未点击其他会话的 AI 分析。全新 Mac 首次读取准备仍是未完成项，发行签名通过不能代替该项产品验收。

## 1.1.2 回复路径完善（2026-09-08）

- 洞察详情先显示结论和建议，再展示统计；无话题时不绘制空话题区；私聊使用“对话氛围”，参与比例改名“谁在说话”。新增“查看对话与回复”，进入最近对话的原文和回复框。
- 草稿新增“继续回复”，持久化带入回复框；遇到不同的未发送内容先确认。单条删除使用原生确认，失败保留草稿。修复消息整行复制实际输出插值语法的问题。
- 发送方法及回车入口增加重入保护；发送成功只清除与发送快照相同的编辑内容，保留等待期间的新编辑。
- 原生预览实测：从洞察进入对话，编辑并保存一条测试草稿；草稿列表出现真实文本；继续回复正确带入；修改草稿后再次继续会出现覆盖确认；取消后原回复仍在；单条删除只删除本轮测试草稿。截图 `design/19-drafts-lifecycle.png`。
- 16 项定向测试通过（回执、持久动作、历史日期），日志 `/tmp/wechathud-reply-flow-tests.log`；Release 68.06 秒、无警告。这是上一轮 792 项全量基线之上的定向验证，未重复宣称本轮全量重跑。
- 1.1.2 公证 `3225f431-5f93-4a47-ac81-17cae2c7df73` Accepted。附票据后独立解压校验签名、Gatekeeper、票据、资源全部通过。最终 SHA-256 `d1cdfcda42a3ec5614ad97d229e4894e9e1dffcf405ba51207bed664f8bb6748`，证据 `distribution-1.1.2-check.json`。
- 已备份 1.1.1 并安装 `/Applications/WeChatHUD.app` 1.1.2。正式 GUI 同步时间 15:20，测试号显示新的结论卡及回复入口，入口正确进入测试号原文。
- 正式发送测试未执行：CUA 填写回复框时连续报告界面已变化，随后浮窗收起。没有点击发送，不能宣称实际发送回执通过。此轮无新增微信消息。
- 全新 Mac 的一步首次读取仍未实现；原生界面及签名改进不替代该要求。完整群 @ 到回复、真实承诺提醒、发送回执与跨设备首次连接仍需各自实证。

## 回复焦点专项（2026-09-08，进行中）

- 1.1.2 复现：进入测试号详情后 AX 能定位回复框；点击后 AX 可显示该输入框为焦点，但粘贴等待应用读取剪贴板超时，未点击发送。
- 代码差异：工作台显式激活应用并前置窗口，浮窗详情仅 `makeKey()`；鼠标离开和新通知都有 detail 保护，没有发现它们直接关闭详情的缺口。
- 最小修复候选：仅进入 detail 时激活 accessory 应用并 `makeKeyAndOrderFront`；compact、extended、notification 焦点策略不变。此处仍为待实测的原因假设，不能以代码差异证明故障已解决。
- Apple 对非激活面板的焦点说明：[NSPanel becomesKeyOnlyIfNeeded](https://developer.apple.com/documentation/appkit/nspanel/becomeskeyonlyifneeded)。显式文本编辑仍需实际键盘/粘贴验证。
- 12 项浮窗交互与回执定向测试通过，日志 `/tmp/wechathud-focus-tests.log`。这些测试不证明真实 OS 输入已恢复。

### 权限状态冲突与恢复提示

- 已安装 1.1.3 的回复输入和发送确认可用；确认后 macOS 权限检查返回未授权，未发出新测试消息。
- 系统设置中 WeChatHUD 的设备控制和数据访问开关实测为开启；尚未证明旧授权绑定或权限缓存是原因。只读系统日志查询无可用记录，未修改 TCC、未重置授权。
- 源码设置页增加重新检查权限和重新打开应用的恢复说明；发送失败文案准确表达当前应用未获得授权。恢复说明不承诺一定能解决，也不自动发送。
- 最后一次 swift build 通过，git diff --check 通过；本次提示改动尚未打包安装或完成原生界面验收。权限异常、发送回执和首次连接完整流程仍待验收。

### 1.1.4 核心消息链路修复

- 群聊补充摘要：保留最近 30 条消息后按正序交给 AIGroupCatchup 和 ContextAnalyzer。GroupContextBriefing 原已有时间排序，本轮仅补充同秒 localId 排序。
- 群聊 service 组装测试使用 30 条模拟消息，验证最新 24 条窗口及同秒先后顺序；GroupContextBriefingServiceTests 与 MessageScanPaginationTests 合计 12 项通过。此测试不直接覆盖 ChatMonitor 私有 catchup 入口。
- 承诺提醒：pending 且已到截止时刻，以及已标记 overdue 的承诺均可提醒；临近与到期阶段使用不同去重标识，继续沿用每小时预算和并发去重。完成、取消承诺不提醒。两项新增测试在旧代码上真实失败，修复后 ProactiveAlertTests 16 项通过。
- 权限错误在回复区内换行展示，并提供直接检查微信操作权限入口；设置页提供重新检查。
- 正式 1.1.3 保持同一签名与安装位置重启后，系统操作权限检查仍未授权；数据库连接正常。重启未解决冲突。未更改系统权限、未发出新消息，原因仍未查实。

- 1.1.4 release 构建 44.24 秒通过，无编译警告；Developer ID 签名，Apple 公证 b2653e60-1272-4a08-880a-1c03de314514 Accepted。独立解压严格签名、公证票据、Gatekeeper 与 bundle-check 全部通过。
- 已安装到 /Applications/WeChatHUD.app。正式原生界面显示微信已连接，16:12 同步；重新检查权限按钮实测返回当前应用仍未获授权，未假报成功。设置页截图已人工查看，无文字裁切。回复区新增错误布局尚未再次触发发送验证，以免把权限修复当成已完成。
- 可分发归档 .build/distribution/WeChatHUD-1.1.4-macOS14-arm64.zip，SHA-256 cd2226ab65146b05307d9a45a07cc05ece5f57b47e7b51b622af6d7792611293。本轮没有再次执行全套测试；28 项相关测试与 release 构建为本轮验证范围。


## 诊断只读边界修正（候选包复核）

后续源码审核发现，既有 `self-check` 虽然只读微信源数据库，但调用 `AccountStoreCoordinator.bootstrap()`，会以正常读写方式打开 HUD 业务库，执行建表、迁移和清理；首次运行还可能初始化设备设置。`WeChatReader.getMessages` 也可能持久化学习到的自身别名。因此本文早先“只读诊断”仅能支持微信源读取方向，不能证明应用自有数据没有改变。历史读数仍是当时输出，不据此声称无副作用。

本轮尚未运行现有 Candidate 包的 `self-check`；已要求将诊断与启动写入分离并补文件不变性回归，再重新构建和执行。既有候选包静态签名和资源检查仍有效，但不包含这项待完成修复。

### 诊断修复与本轮候选包验证结果

`self-check` 已与正常启动分离：只读设备配置，旧配置需要时从私有临时副本读取；原业务库不以 SQLite 打开。存在 WAL/journal 时业务统计不可用，复制前后校验源稳定；临时目录 0700、副本 0600，关闭后清理。诊断实例不会持久化自身别名。

23 项 AccountStoreCoordinator / ProductSelfCheck / ReaderCacheIdentity 测试通过，实际退出码 0。覆盖静态库非空读取、旧配置非默认路径回退、原库字节和目录不变、临时快照清理、活动 WAL 跳过、损坏配置拒绝，以及别名学习持久化正反对照。Release 构建实际退出码 0（46.22 秒）；重新打包签名、严格 codesign 验证和 bundle-check 均通过。

更新候选包执行 `self-check` 实际退出码 0：会话 2,724、联系人 12,323，第一条会话探测读取到消息，失败 0。证据 `candidate-live-source-check.json`。旧业务记录为 account_confirmation_required，业务统计如实 unavailable。本轮未启动候选 GUI、未修改系统权限、未调用 AI、未发送消息，也未安装或公证更新候选包。该证据仅验证现有已配置账号读取，不证明新 Mac 首次连接、控制权限或真实发送回执。
